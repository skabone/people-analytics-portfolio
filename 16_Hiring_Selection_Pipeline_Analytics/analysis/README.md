# Analysis

This folder contains a lightweight Python analysis that:

- loads the synthetic CSVs in `../data/` into a local SQLite database
- runs the SQL in `../sql/`
- writes derived tables and a short results snapshot

## Run

```bash
python3 analysis/run_analysis.py
```

